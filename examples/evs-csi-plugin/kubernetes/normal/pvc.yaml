apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: evs-normal-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: evs-sc
