apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: evs-normal-resize-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  storageClassName: evs-sc
